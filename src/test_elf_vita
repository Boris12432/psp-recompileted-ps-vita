#include "vita_elf.h"

#include <cstdio>

int main()
{
    VitaELF elf;

    if (!elf.load("game.elf"))
    {
        std::printf(
            "ELF loading failed\n"
        );

        return 1;
    }

    std::printf(
        "ENTRY = 0x%08X\n",
        elf.entryPoint()
    );

    return 0;
}